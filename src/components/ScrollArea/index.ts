/**
 * ScrollArea Component - Token-driven custom scrollbar wrapper
 */

export { ScrollArea } from './ScrollArea'
export type {
  ScrollAreaProps,
  ScrollAreaMode,
  ScrollAreaVisibility,
  ScrollAreaOrientation,
} from './ScrollArea'
