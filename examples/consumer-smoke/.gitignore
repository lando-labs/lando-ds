node_modules/
# ds.tgz is the packed DS tarball, produced by CI (`npm pack`) — never committed.
*.tgz
ds.tgz
# Not committed: the DS is a file: tarball re-packed each CI run, so a pinned
# integrity hash here causes EINTEGRITY. The harness installs fresh.
package-lock.json
